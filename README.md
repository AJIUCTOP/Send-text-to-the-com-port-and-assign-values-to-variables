# Send-text-to-the-com-port-and-assign-values-to-variables
we send text to the com port and assign values ​​to variables, theoretically it is possible to make an exchange between 2 microcontrollers, and thus combine 2 programs
when sending R1G1B1 to the COM port, the diode on pin 13 of the Arduino will blink, since the condition for B1 = 1 is specified
